import { NameLettersPipe } from './name-letters-pipe';

describe('NameLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new NameLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
